var=100
print(id(var))
# 140736988581896

# id(var) returns the memory address (or a unique identifier) where the integer object 100 is stored.
# This value is typically represented as a large integer (like 140736988581896 in your case).
# The exact value will differ between different Python runs or systems.

